# شبكة العلامة الشيخ منير الخباز - البوابة العامة (Almoneer General Portal & CMS)

بوابة إلكترونية متكاملة ونظام إدارة محتوى متطور (CMS) للموقع الرسمي لسماحة العلامة السيد منير الخباز، مبني باستخدام **Laravel 12** وبتصميم إسلامي فاخر يدعم الواجهتين العصرية والتراثية، ومتوافق بنسبة 100% مع الهواتف الذكية وأجهزة القراءة والطباعة.

---

## 📌 الفهرس
1. [المميزات والمحتويات](#-المميزات-والمحتويات)
2. [التقنيات المستخدمة](#-التقنيات-المستخدمة)
3. [البنية التحتية واستضافة الخادم (Hetzner)](#-البنية-التحتية-واستضافة-الخادم-hetzner)
4. [إعداد إصدار PHP عبر htaccess (بالغ الأهمية)](#-إعداد-إصدار-php-عبر-htaccess-بالغ-الأهمية)
5. [أداة تنقيح وتفريغ المحاضرات (المخمة - Vacum Tool)](#-أداة-تنقيح-وتفريغ-المحاضرات-المخمة---vacum-tool)
6. [نظام تشغيل الصوتيات والمرئيات](#-نظام-تشغيل-الصوتيات-والمرئيات)
7. [لوحة التحكم والإدارة (Admin CMS)](#-لوحة-التحكم-والإدارة-admin-cms)
8. [طريقة التشغيل المحلي (Local Development)](#-طريقة-التشغيل-المحلي-local-development)
9. [طريقة النشر والمزامنة مع الخادم الحي (Deployment)](#-طريقة-النشر-والمزامنة-مع-الخادم-الحي-deployment)

---

## 🌟 المميزات والمحتويات

* **المحاضرات والمواسم السنوية:**
  * تنظيم متسلسل حسب السنوات الهجرية والمواسم (عاشوراء، صفر، شهر رمضان، الوفيات والمواليد).
  * توفير كل محاضرة بثلاث صيغ متكاملة:
    1. **فيديو مرئي:** مشغل YouTube ذكي بدون إعلانات وتأخير.
    2. **تسجيل صوتي:** مشغل مدمج بالموقع بصيغة MP3، أو تضمين مباشر من SoundCloud مع مشغل عائم يتنقل مع الزائر أثناء القراءة.
    3. **تفريغ نصي منقح ومطابق للطباعة:** عرض نصي scholarly بالخط العثماني للآيات، مع أزرار تكبير/تصغير الخط، ونسخ النص، وزر طباعة مخصص يخفي عناصر الموقع ويخرج مستنداً منسقاً يطابق ملفات الـ PDF الأصلية.
* **المؤلفات والكتب:** مكتبة رقمية للكتب الفقهية، العقائدية، والفكرية مع إمكانية التحميل المباشر وتصفح الفهارس.
* **ديوان الشعر:** عارض تفاعلي لقصائد سماحة السيد ببحورها الشعرية ومناسباتها.
* **الاستفسارات الشرعية والفكرية:** أرشيف إجابات سماحة السيد مصنفة فقهياً وعقائدياً.
* **معرض الصور والميديا:** ألبومات توثيقية للزيارات والأنشطة التبليغية في مختلف دول العالم.

---

## 🛠 التقنيات المستخدمة

* **إطار العمل (Backend):** Laravel 12 (PHP 8.2 / 8.3).
* **قاعدة البيانات:** SQLite (`database/database.sqlite`) لسرعة الأداء وسهولة النقل والنسخ الاحتياطي المستقل.
* **الواجهة الأمامية (Frontend):** Blade Templates، Tailwind CSS (مع خيارات الثيم الزمردي والثيم الفيروزي المشرق)، خطوط عربية فاخرة (IBM Plex Sans Arabic للواجهات، Amiri للنصوص العلمية والشعر، و Amiri Quran / KFGQPC Uthman Taha للآيات القرآنية).
* **إدارة الحزم:** Composer & NPM.

---

## 🌐 البنية التحتية واستضافة الخادم (Hetzner)

* **الخادم:** استضافة Hetzner المدارة (`195.201.6.46:222`).
* **استراتيجية العزل والحماية (Zero-Downtime Isolation):**
  * الموقع القديم ورسالة الصيانة تعمل في المجلد الرئيسي: `/usr/www/users/almonc/`.
  * التطبيق المطور تم وضعه في مجلد مستقل ومعزول تماماً: `/usr/www/users/almonc/almoneer_app/`.
  * يتم استعراض واختبار الموقع المطور عبر الرابط الفرعي: **`https://www.almoneer.org/home/`**
  * تم ربط الرابط عبر Symlink: `/usr/www/users/almonc/home` $\rightarrow$ `/usr/www/users/almonc/almoneer_app/public`.

---

## ⚙️ إعداد إصدار PHP عبر .htaccess (بالغ الأهمية)

على خوادم Hetzner المدارة (konsoleH)، يعمل خادم Apache الافتراضي على مستوى النطاق بإصدار قديم (PHP 5.6) للموقع القديم. ونظراً لأن متطلبات Laravel 12 تحتاج إلى `PHP >= 8.2`، تم ضبط ملف `public/.htaccess` بتوجيه مخصص يحقن **PHP 8.3 FastCGI** لمجلد التطبيق حصرياً:

```apache
<IfModule mod_fcgid.c>
    # تشغيل PHP 8.3 حصرياً لتطبيق المنير المطور عبر FastCGI
    Options +ExecCGI -MultiViews -Indexes

    <FilesMatch "\.php$">
        SetHandler fcgid-script
        FCGIWrapper /home/httpd/cgi-bin/php83-fcgi-starter.fcgi .php
    </FilesMatch>
</IfModule>

<IfModule !mod_fcgid.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /home/

    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
```

> **تنبيه:** هذا الملف محفوظ ومحدث داخل المستودع (`public/.htaccess`). عند عمل أي تحديث أو سحب من Git (`git pull`)، يتم الحفاظ عليه تلقائياً لضمان عدم توقف الموقع أو ظهور خطأ Composer Platform.

---

## 🧹 أداة تنقيح وتفريغ المحاضرات (المخمة - Vacum Tool)

الأداة المسؤولة عن معالجة وتدقيق النصوص المفرغة المنقولة من Word أو قواعد البيانات السابقة:
* **المسار في لوحة التحكم:** `http://127.0.0.1:8000/admin/tools/vacum`
* **الفئة البرمجية المسؤولة:** `App\Services\TranscriptFormatter`

### قواعد التنظيف والتنسيق الآلي:
1. **معالجة النصوص المشفرة وكسر الأسطر:**
   * فك تشفير رموز HTML (مثل `&#1648;` للألف الخنجرية في الآيات و `&#1750;` لعلامات الوقف).
   * إزالة علامات الكسر الحرفية `\n` و `\t` الناتجة عن تفريغ قواعد البيانات.
2. **الآيات القرآنية:**
   * تحويل `{الآية}` و `﴿الآية﴾` إلى الخط العثماني مع لون قرمزي فاخر (`#BB1111`) وحجم خط متناسق مع المتن بنسبة `1.05x`.
3. **العناوين والمحاور الرئيسية:**
   * تحويل وسوم `<h5>`, `<h4>`, `<h3>` ووسوم `[t]عنوان[/t]` إلى خط عريض باللون الأحمر الداكن (`#990000`) وفواصل رأسية مريحة للعين.
   * العناوين الفرعية والمصاديق تظهر باللون البني المعتمد (`#8B4513`).
4. **القوائم النقطية للمحاور:**
   * تحويل `<ul>` و `<li>` إلى نقاط بارزة `list-disc` مع تباعد متناسق وخط عريض، مطابقة تماماً لملفات الـ PDF الصادرة عن شبكة المنير.
5. **الصلوات والأدعية:**
   * استبدال `(ص)` برسم المصحف الشريف للفظ النبوي الشريف: **`ﷺ`**.
   * تنسيق رموز الأئمة: `(ع)`، `(عع)`، `(عه)` بألوان ذهبية وقورة وخطوط واضحة.

---

## 🎧 نظام تشغيل الصوتيات والمرئيات

في نموذج إضافة وتعديل المحاضرات (`/admin/media/{id}/edit`):
* **الصيغة الصوتية المباشرة (MP3):** رفع ملف صوتي يتم تخزينه في `public/uploads/audio/`، أو وضع رابط صوتي خارجي مباشر (مثل `https://www.almoneer.org/sounds/...`).
* **رابط ساوندكلاود (SoundCloud):** إمكانية وضع رابط المقطع من ساوندكلاود، حيث يتعرف النظام عليه آلياً ويقوم بتضمين مشغل SoundCloud الرسمي بشكل مدمج.

---

## 📱 لوحة التحكم والإدارة (Admin CMS)

* **الرابط:** `/home/admin` (أو `/admin` محلياً).
* **التوافق التام مع الجوال:**
  * قائمة ملاحة منسدلة خاصة بالهواتف الذكية مع Backdrop مظلل.
  * جداول البيانات قابلة للتمرير الأفقي بسلاسة (`overflow-x-auto`).
  * بطاقات إحصائية متكيفة ومريحة للاستخدام باللمس.

---

## 💻 طريقة التشغيل المحلي (Local Development)

```bash
# 1. تثبيت الحزم
composer install
npm install

# 2. إعداد قاعدة البيانات وتوليد المفتاح
cp .env.example .env
php artisan key:generate
touch database/database.sqlite
php artisan migrate --seed

# 3. تشغيل السيرفر المحلي
php artisan serve
# أو
php -S 127.0.0.1:8000 -t public
```

---

## 🚀 طريقة النشر والمزامنة مع الخادم الحي (Deployment)

لإرسال وتطبيق التحديثات على الخادم دون التأثير على استقرار الموقع:

```bash
# 1. حفظ التغييرات ودفعها إلى GitHub
git add .
git commit -m "Your descriptive commit message"
git push origin main

# 2. تشغيل سكربت النشر الآمن عبر بايثون
python scratch/deploy_and_seed_remote.py
```

يقوم السكربت آلياً بـ:
1. الاتصال الآمن بالخادم عبر SSH على المنفذ `222`.
2. سحب آخر التعديلات من Git (`git pull origin main`).
3. التأكد من ثبات ملف `.htaccess` الخاص بـ FastCGI PHP 8.3.
4. رفع ملفات الـ PDF والتفريغات الصوتية عبر SFTP.
5. تفريغ كاش القوالب والتطبيق (`php artisan view:clear && cache:clear`).
6. فحص حالة الروابط الحية والتأكد من إرجاع كود `200 OK`.
