# دليل التوصيف المعماري والتقني لمنظومة موقع العلامة المنير (العام)
# CMS Architecture & Technical Specifications

**اسم المشروع:** البوابة العامة والفكرية لسماحة العلامة السيد منير الخباز  
**المسار المحلي:** `C:\wamp\www\almoneer-gen`  
**المنصة الشقيقة (التخصصية):** بوابة الدروس الحوزوية والبحث الخارج (`C:\wamp\www\almoneer` / `http://localhost:8000`)  
**الأنظمة المرجعية:** `assfar-laravel`, `salma`, `qatarim`, `mawkeb-laravel`, `kalema-laravel`  

---

## 1. فهرس المحتويات
1. المعمارية العامة واستراتيجية فصل المنصتين (Two-Portal Strategy)
2. مواصفات وميزات الأقسام العامة (Public Facing Features)
3. ميزات لوحة تحكم إدارة المحتوى (CMS Admin Features)
4. إدارة القوائم الأفقية والعمودية (Horizontal & Vertical Menu System)
5. إدارة القوالب والثيمات (Theme Management System)
6. استراتيجية التسريع والتخزين المؤقت (High-Performance Caching & Admin Invalidation)
7. دليل الهوية البصرية والتصميم المتجاوب (Design System & Mobile-First UX)

---

## 2. المعمارية العامة واستراتيجية فصل المنصتين (Two-Portal Strategy)

لتحقيق أقصى درجات التركيز والتجربة السلسة لمختلف فئات الزوار، تنقسم المنظومة إلى بوابتين متكاملتين:

```
                  ┌─────────────────────────────────────────┐
                  │    بوابة العلامة السيد منير الخباز       │
                  └────────────────────┬────────────────────┘
                                       │
            ┌──────────────────────────┴──────────────────────────┐
            ▼                                                     ▼
┌──────────────────────────────────────┐  ┌──────────────────────────────────────┐
│  الموقع العام والفكري (almoneer-gen)  │  │ بوابة الدروس الحوزوية (almoneer)    │
│  الرابط: almoneer.org                │  │ الرابط: droos.almoneer.org (Port 8000)│
├──────────────────────────────────────┤  ├──────────────────────────────────────┤
│ • عامة الجمهور، الشباب، المتابعون     │  │ • طلاب العلوم الدينية، الباحثون      │
│ • المحاضرات العامة والمواسم          │  │ • أبحاث الخارج (فقه وأصول ورجال)     │
│ • الكتب والمؤلفات، الشعر، الأخبار   │  │ • جداول وتقاويم الدروس الحوزوية      │
│ • الاستفسارات، الفتاوى، الميديا     │  │ • نصوص وتفريغات المقررات الأكاديمية  │
└──────────────────────────────────────┘  └──────────────────────────────────────┘
            ▲                                                     ▲
            └──────── شريط التبديل العلوي الدائم (Header Switcher) ────────┘
```

---

## 3. مواصفات وميزات الأقسام العامة (Public Facing Features)

### 1. الرئيسية (Home)
- **الهيدر التفاعلي (Hero Showcase):** بطاقة بارزة بتأثير زجاجي راقٍ تعرض أحدث المواسم (مثل موسم عاشوراء 1447هـ أو شهر رمضان)، مع صورة عالية الدقة لسماحة السيد وخيارات استماع أو مشاهدة مباشرة.
- **شريط البث المباشر (Live Broadcast Bar):** يظهر ديناميكياً عند تفعيل البث، مع توفير مؤقت عد تنازلي للمحاضرات القادمة وروابط البث على يوتيوب ومخدمات الصوت.
- **كلمة الأسبوع وقبسات أمير المؤمنين (ع):** استمرار للميزة التاريخية الأثيرة لدى زوار الموقع القديم، مصممة كبطاقة اقتباس بخط عربي فاخر مع زر مشاركة فوري للواتساب ومنصات التواصل.
- **شبكة الوسائط والجديد (Latest Content Tabs):** تبديل سلس بين (أحدث المحاضرات الصوتية، أحدث المقاطع المرئية، أحدث الأخبار والبيانات).
- **روابط البوابات السريعة (Quick Portals):** بطاقات وصول مباشر إلى: مكتبة الكتب، ديوان الشعر، وبوابة الدروس الحوزوية.

### 2. نبذة عن حياته (Biography)
- مسار زمني تفاعلي (Interactive Timeline) لمراحل حياته الشريفة:
  - النشأة والطلب العلمي المبكر.
  - مرحلة النجف الأشرف وقم المقدسة وكبار أساتذته.
  - إجازات الاجتهاد وشهادات كبار مراجع الطائفة.
  - الإنتاج الفكري والمشروع التبليغي العالمي.
- معرض صور ووثائق تاريخية نادرة عالية الدقة مدمجة مع التواريخ.
- إمكانية تنزيل السيرة الذاتية الرسمية كملف PDF بتصميم مطبوع راقٍ.

### 3. الصوتيات (Audio Library)
- **مشغل صوتي مستمر عائم (Floating Persistent Audio Player):** يظل يعمل في أسفل المتصفح أثناء تجول الزائر بين المقالات والصفحات بدون توقف الصوت.
- **محاضرات المواسم والسلاسل:** تصنيف دقيق (محاضرات عاشوراء بحسب السنوات، شهر رمضان، خطب الجمعة، محاضرات السيرة والتربية).
- **خصائص المشغل:** تسريع الصوت (1x, 1.25x, 1.5x)، التقديم والتأخير 10 ثوانٍ، زر تحميل مباشر (MP3)، وعرض تفريغ المحاضرة المكتوب أسفل المشغل.

### 4. المرئيات (Video Library)
- مشغل فيديو مدمج متوافق مع كافة الشاشات ويعتمد التحميل الكسول (Lazy-loading) لضمان سرعة التصفح.
- قسم مخصص للمقاطع القصيرة (Shorts / Reels) بتنسيق طولي مناسب للهواتف الذكية.
- تصنيفات المرئيات: الحوارات التلفزيونية، الندوات الفكرية، والمجالس الحسينية.

### 5. شعر (Poetry & Elegies)
- ديوان القصائد الولائية والرثائية والوجدانية المنظومة بقلم سماحة السيد.
- تنسيق "الصدر والعجز" متجاوب تماماً، بحيث يظل متناسقاً على شاشات الجوال الصغيرة دون تداخل الكلمات.
- إمكانية تشغيل الإلقاء الصوتي للقصيدة إن توفر.
- فلترة القصائد حسب البحر الشعري أو المناسبة (في النبي وآله، مراثي عاشوراء، رثاء الأعلام).

### 6. كتب ومؤلفات (Books & Publications)
- استعراض أغلفة الكتب بتأثير ثلاثي الأبعاد راقٍ (3D Book Card Effect).
- قارئ إلكتروني مدمج (Embedded PDF Flipbook / Viewer) لقراءة الكتاب مباشرة داخل الموقع.
- تحميل الكتاب بجودة عالية أو جودة مخففة مخصصة للهواتف.
- فهرس تفصيلي لكل كتاب، وروابط لدور النشر المعتمدة أو أماكن التوزيع.

### 7. أخبار ونشاطات (News & Activities)
- تغطية الزيارات والجولات التبليغية في مختلف الدول (أمريكا، بريطانيا، العراق، دول الخليج).
- البيانات والمواقف الرسمية الصادرة من مكتب سماحة السيد.
- تصفية الأخبار حسب التواريخ أو التصنيفات، مع محرك بحث فوري.

### 8. ألبوم وصور (Photo Gallery)
- ألبومات صور مصنفة حسب المناسبات والسنوات.
- عارض صور حديث (Lightbox) يدعم السحب باللمس (Touch & Swipe) وزوم وتنزيل الصور بدقة فائقة.

### 9. استفسارات (Inquiries & Q&A)
- **أرشيف الأسئلة المجابة (Public Q&A Archive):** محرك بحث وفلترة للأسئلة الفقهية، العقائدية، القرآنية، والاجتماعية.
- **نموذج إرسال استفسار خاص:** حقول سهلة ومحمية مع خيار تحديد رغبة السائل في نشر السؤال دون الاسم أو الاحتفاظ بالسرية، وتوليد رقم تتبع خاص.

### 10. تواصل (Contact Us)
- نموذج تواصل محمي بـ Google reCAPTCHA v3 غير المرئي.
- إرسال فوري إلى البريد المعتمد للمكتب عبر Laravel Mailable.
- عناوين المكاتب وساعات الاستقبال الرسمية وأرقام الهواتف الرسمية.

### 11. تواصل .. ميديا (Social Media Hub)
- شريط أيقونات وروابط مباشرة للحسابات الرسمية الموثقة (YouTube, Instagram, TikTok, X, Telegram).
- قسم تفاعلي يعرض أحدث المنشورات والفيديوهات لجذب فئة الشباب والناشئة.

---

## 4. ميزات لوحة تحكم إدارة المحتوى (CMS Admin Features)

تعتمد لوحة التحكم على نفس المعمارية المتقدمة المستخدمة في **assfar-laravel** و **salma** و **qatarim**:

1. **نظام الصلاحيات وتعدد المستخدمين (Roles & Permissions):**
   - مدير عام (Super Admin): تحكم كامل في القوائم والثيمات والإعدادات والنسخ الاحتياطي.
   - محرر محتوى (Editor): إضافة وتعديل المقالات والصوتيات والمرئيات والكتب.
   - مسؤول الاستفسارات (Inquiries Moderator): استقبال وفرز الأسئلة وإرسال الإجابات.
2. **سجل رقابة وتتبع العمليات (Audit Logging Trait):**
   - تسجيل كل حركة في جدول `audit_logs` (المستخدم، الـ IP، نوع العملية: إضافة/تعديل/حذف، الوحدة، وتاريخ ووقت العملية).
3. **إدارة المحاضرات والصوتيات والمرئيات:**
   - جلب بيانات يوتيوب تلقائياً بمجرد إدخال الرابط (العنوان، الوصف، الصورة المصغرة، المدة).
   - رفع ملفات الـ MP3 وحفظها مع تحديد المواسم والسلاسل.
4. **نظام معالجة الاستفسارات (Inquiry Triage Engine):**
   - فرز الأسئلة حسب الحالة: (جديد ➜ قيد المراجعة ➜ تم الرد الخاص ➜ معتمد للنشر في الأرشيف العام).
   - إرسال الإجابة آلياً إلى بريد المستفسر مع الحفاظ على خصوصيته.
5. **التحكم في البث المباشر (Livestream Switcher):**
   - تبديل حالة الموقع إلى (مباشر الآن) مع تحديد رابط البث وعنوان المحاضرة.
6. **زر تفريغ الكاش المباشر (One-Click Cache Flush):**
   - زر سريع ومثبت في الشريط العلوي للمدير لتفريغ كاش الصفحات والاستعلامات فوراً عند الحاجة.

---

## 5. إدارة القوائم الأفقية والعمودية (Menu Management System)

بناءً على طلب العميل ومطابقة لتجربة `almoneer` و `kalema-laravel`:

### المسار والتخزين
- **القائمة الأفقية (Horizontal Menu):** تُحفظ وتُدار عبر `storage/app/menus/menu.htm`.
- **القائمة العمودية (Vertical Menu):** تُحفظ وتُدار عبر `storage/app/menus/menuv.htm`.

### واجهة التحكم التفاعلية (`MenuController`)
- **لوحة السحب والإفلات (SortableJS Drag & Drop):** ترتيب العناصر وإنشاء القوائم الفرعية (Submenus) بصرياً.
- **معالج إضافة العناصر (Menu Item Wizard):**
  - إضافة رابط مخصص (Custom Link).
  - أو ربط مباشر بتصنيف مقالات (Article Category).
  - أو ربط بألبوم صور (Gallery Category).
  - أو ربط بمكتبة ميديا (Media Category).
  - أو ربط بصفحة ثابتة (نبذة عن حياته، ديوان الشعر، استفسارات، تواصل).
- **التخزين المؤقت الفوري:** عند حفظ أي من القائمتين، يقوم النظام آلياً بتفريغ مفاتيح الكاش الخاصة بالقوائم (`site.menus.horizontal` و `site.menus.vertical`) لتحديث الموقع مباشرة لجميع الزوار.

---

## 6. إدارة القوالب والثيمات (Theme Management System)

مستوحى من معمارية `ThemeController` المطبقة في مشاريع WAMP السابقة:

### الهيكلية
- **مجلد الثيمات:** `public/site_assets/`
- يحتوي كل ثيم على مجلد مستقل يضم ملف التوصيف: `theme.json`، مثال:
  ```json
  {
    "name": "almoneer-emerald",
    "title": "الثيم الزمردي الملكي",
    "version": "1.0.0",
    "author": "Almoneer Tech Team",
    "description": "الثيم الرسمي المتناسق مع الهوية الحوزوية العريقة والمظهر الحديث",
    "colors": {
      "primary": "#0a3d47",
      "accent": "#c5942d",
      "background": "#faf9f5"
    }
  }
  ```
### الوظائف الإدارية
- **استعراض الثيمات:** شاشة تعرض بطاقات الثيمات المتوفرة مع صورة مصغرة لكل ثيم ومؤشر على الثيم النشط حالياً.
- **تفعيل الثيم بنقرة واحدة:** حفظ اسم الثيم النشط في جدول الإعدادات `settings` تحت المفتاح `site.theme`.
- **رفع ثيم جديد (ZIP Upload):** إمكانية رفع حزمة مضغوطة وفك ضغطها آلياً في مسار `public/site_assets`.
- **حذف وتعديل الثيمات غير النشطة.**
- **التحديث التلقائي:** تفعيل أي ثيم يفرغ كاش الإعدادات والقوالب فوراً.

---

## 7. استراتيجية التسريع والتخزين المؤقت (High-Performance Caching & Admin Invalidation)

### الفلسفة التشغيلية
الهدف هو تحقيق **أقصى سرعة ممكنة (Near-Instant TTFB)** لجميع زوار الموقع عبر تقليل الاستعلامات على قاعدة البيانات إلى ما يقارب الصفر أثناء التصفح العادي، مع ضمان **تحديث المحتوى فورياً دون أي تأخير عند قيام الإدارة بأي تعديل**.

### آلية العمل التقنية

```
[ الزائر العام ] ───► [ فحص الكاش ] ───(متوفر؟)───► [ تقديم الصفحة فوراً < 50ms ]
                                  │ (غير متوفر)
                                  ▼
                         [ تنفيذ الاستعلام من DB ]
                                  │
                         [ حفظ النتيجة في الكاش ]

[ لوحة التحكم / المدير ] ───► [ إجراء تعديل / حفظ / حذف ]
                                  │
                                  ▼
                         [ تفعيل Trait / Observer ]
                                  │
                         [ تفريغ المفاتيح المتأثرة فوراً ] ───► [ الزيارة القادمة تجلب الجديد ]
```

### 1. المفاتيح المخزنة (Cache Keys Architecture)
- `site.settings`: إعدادات الموقع وروابط التواصل والهوية.
- `site.menus.horizontal`: كود القائمة العلوية المحفوظة.
- `site.menus.vertical`: كود القائمة الجانبية المحفوظة.
- `site.theme.active`: الثيم المفعل حالياً.
- `site.home.payload`: مصفوفة بيانات الصفحة الرئيسية (المحاضرة البارزة، كلمة الأسبوع، أحدث الصوتيات والمرئيات، الكتب المميزة).
- `site.articles.recent`: أحدث المقالات والأخبار.
- `site.wisdom.active`: كلمة الأسبوع الحالية.
- `site.books.featured`: قائمة الكتب المعروضة.

### 2. الإلغاء التلقائي عند التعديل (Automatic Invalidation via Traits)
إنشاء Trait موحد `App\Traits\ClearsFrontendCache`:
```php
namespace App\Traits;

use Illuminate\Support\Facades\Cache;

trait ClearsFrontendCache
{
    public static function bootClearsFrontendCache()
    {
        static::saved(function ($model) {
            static::invalidateCache($model);
        });

        static::deleted(function ($model) {
            static::invalidateCache($model);
        });
    }

    public static function invalidateCache($model)
    {
        // مسح كاش الصفحة الرئيسية دائماً
        Cache::forget('site.home.payload');
        
        // مسح الكاش بحسب نوع الموديل
        if ($model instanceof \App\Models\Setting) {
            Cache::forget('site.settings');
            Cache::forget('site.theme.active');
        } elseif ($model instanceof \App\Models\WeeklyWisdom) {
            Cache::forget('site.wisdom.active');
        } elseif ($model instanceof \App\Models\Book) {
            Cache::forget('site.books.featured');
        } elseif ($model instanceof \App\Models\Article) {
            Cache::forget('site.articles.recent');
        }
    }
}
```
تطبيق هذا الـ Trait على كل من: `Article`, `MediaItem`, `Book`, `WeeklyWisdom`, `GalleryItem`, `Inquiry`, `Setting`.

---

## 8. دليل الهوية البصرية والتصميم المتجاوب (Design System & Mobile-First UX)

### فلسفة التصميم: "الوقار المعرفي والروحانية المعاصرة"
الابتعاد التام عن التصاميم البدائية أو الباهتة؛ وتقديم واجهة تشبه المنصات الفكرية العالمية مع الاحتفاظ بالروح الإسلامية الحوزوية الرفيعة.

### لوحة الألوان الأساسية
- **الزمردي الداكن الملكي (Primary Dark Emerald):** `#0a3d47` و `#0f4c5c`
- **الذهبي الملكي المطفي (Accent Royal Gold):** `#c5942d` و `#d4aa48`
- **خلفيات الصفحات العاجية واللؤلؤية (Surface & Canvas):** `#faf9f5` و `#ffffff`
- **نصوص القراءة المريحة (Dark Slate Typography):** `#1e293b`
- **الحدود والزخارف الشفافة (Subtle Borders):** `rgba(197, 148, 45, 0.2)`

### منظومة الخطوط (Typography)
- **خط العناوين والمخطوطات والشعر:** `Amiri` و `Aref Ruqaa` (خط أصيل مخصص للمتون والشعر وعناوين الأبواب).
- **خط واجهات الاستخدام والجوال:** `IBM Plex Sans Arabic` (خط فائق الوضوح ومريح للقراءة السريعة على جميع الهواتف).

### عناصر الواجهة للهواتف الذكية (Mobile-First Components)
1. **شريط التنقل السفلي الثابت (Sticky Mobile Bottom Bar):**
   - الرئيسية 🏠
   - الصوتيات 🎙️
   - البث المباشر 🔴
   - الكتب 📚
   - القائمة ☰
2. **درج المشغل الصوتي العائم (Collapsible Audio Drawer):**
   - شريط مدمج يظهر أسفل الشاشة عند تشغيل أي محاضرة.
   - إمكانية السحب للأعلى ليتحول إلى مشغل كامل يضم كلمات المحاضرة وصورة الغلاف وأزرار التقديم والتأخير.
3. **تنسيق الشعر المتجاوب (Responsive Couplet):**
   - تحويل شطري البيت (الصدر والعجز) في الشاشات الصغيرة إلى عرض عمودي أنيق مع رمز فاصل فاخر بينهما، دون كسر النص، مع المحاذاة الكاملة (Justification) وتوسيع الفاصل الأوسط لمحاكاة طباعة الدواوين.
4. **شعار وبوابة التبديل (Header Portal Switcher):**
   - زر بارز في أعلى الصفحة: `🏛️ بوابة الدروس الحوزوية والبحث الخارج ⇄` ينقل الزائر فوراً إلى منصة الدروس على الرابط المخصص.

---

## 9. نظام تنقيح وتنسيق النصوص وخط الرموز التكريمية (Vacum & Abo-Thar Calligraphic Symbols)

تم تجهيز أداة التحرير والتنقيح المركزية **«المخمة»** (`TranscriptFormatter`) ونظام العرض في الموقع بخط الويب المعتمد **Abo-Thar** لاستبدال الأدعية والعبارات التكريمية تلقائياً:

- **الملفات:** `public/fonts/abo-thar.woff2` (64KB)، `abo-thar.woff`، `abo-thar.ttf`.
- **الفئة المعتمدة:** `.symbol-abothar` المعرفة في قوالب `app.blade.php`, `admin.blade.php`, `vacum.blade.php`.
- **الرموز المعتمدة:**
  - `عليه السلام` ⟵ الحرف `A` (`<span class="symbol-abothar" title="عليه السلام">A</span>`).
  - `عليها السلام` ⟵ الحرف `C` (`<span class="symbol-abothar" title="عليها السلام">C</span>`).
  - `عليهم السلام` ⟵ الحرف `D` (`<span class="symbol-abothar" title="عليهم السلام">D</span>`).
  - `رحمه الله` ⟵ الحرف `6` (`<span class="symbol-abothar" title="رحمه الله">6</span>`).
  - `قدس سره` ⟵ الحرف `H` (`<span class="symbol-abothar" title="قدس سره">H</span>`).
  - `دام ظله` ⟵ الحرف `K` (`<span class="symbol-abothar" title="دام ظله">K</span>`).
  - `(ص)` ⟵ رمز الصلاة النبوية التامة الفيكتور `/images/prophet-pbuhaf.svg`.
- **التوثيق التفصيلي وجدول التحويل الكامل:** راجع الملف المخصص [docs/TRANSCRIPT_VACUM_AND_SYMBOLS.md](docs/TRANSCRIPT_VACUM_AND_SYMBOLS.md).
